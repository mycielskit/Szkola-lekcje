#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);//nitro
    float a;
    while(cin >> a)//dunno how many inputs there are ¯\_(ツ)_/¯
        printf("%.2f", (a*a*3.14));//you guessed it, circle area
    return 0;
}